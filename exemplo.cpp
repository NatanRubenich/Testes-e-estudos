#include <stdio.h>
float salario = (998 *2);

float pagamento(int salario){
  	salario += (quant*50) + (quant(salario*(5/100));
  	return (salario);
}

int main(){
    
  int quant,acu=0;
  float salario, carro[10];
  printf("Informe a quantidade de carros vendidos: ");
  scanf("%d", &quant);
  for(int i=1; i<=quant; i++){
		printf("Informe o valor do %d carro: ", i);
  		scanf("%f", (&carro[i] + (carro[i * (5/100)]));
  		acu+=carro[i];
  }
  pagamento(salario);

  return 0;
}
